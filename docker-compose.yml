services:
  hdu-sniper:
    build: .
    image: hdu-library-sniper:latest
    restart: unless-stopped
    ports:
      - "${HDU_WEB_PORT:-8000}:8000"
    environment:
      HDU_SNIPER_HOME: /var/lib/hdu-sniper
    volumes:
      - hdu-data:/var/lib/hdu-sniper

volumes:
  hdu-data:
